import React from 'react'

const FilterComponent = (props) => {
  const {content, key, title} = props
  return (
        <div>
            {content}
        </div>
  )
}

export default FilterComponent
